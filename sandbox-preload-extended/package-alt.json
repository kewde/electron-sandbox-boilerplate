/*
  Alternative package.json.
  If you don't like the way that the electron is being launched in the example then you can 
  delete: package.json & index.js
  and rename this to the new package.json
  Also: delete this comment section.
*/
{
  "name": "sandbox-preload-extended",
  "version": "1.0.0",
  "description": "",
  "main": "./electron/main.js",
  "scripts": {
    "start": "electron --enable-sandbox ./electron/main.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kewde",
  "license": "MIT",
  "devDependencies": {
    "electron": "^1.7.12"
  }
}
